"use strict";
const arrayOfNumbers = [1, 2, 3, 4];
const arrayOfString = arrayOfNumbers.map((number) => number.toString());
console.log(arrayOfString);
const area1 = { height: 4, width: 5 };
const reactangularArea = {
    height: 30,
    width: 40
};
